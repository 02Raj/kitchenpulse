# Copy to application-local.yml (gitignored) for local MongoDB + Redpanda on the host.
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/kitchenpulse_notify
  kafka:
    bootstrap-servers: localhost:19092

kitchenpulse:
  jwt:
    secret: kitchenpulse-dev-only-change-me-32bytes-min
